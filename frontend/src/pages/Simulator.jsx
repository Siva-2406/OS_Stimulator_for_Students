import { useParams } from 'react-router-dom';
import { Play, RotateCcw, Save, Lightbulb, Settings2 } from 'lucide-react';
import { modules } from '../data/modules';
export default function Simulator(){const {moduleId}=useParams(); const m=modules.find(x=>x.id===moduleId)||modules[0]; return <div className="page"><div className="sim-header"><div><span className="kicker">{m.topic}</span><h2>{m.name}</h2><p>{m.description}</p></div><div className="sim-actions"><button className="btn secondary"><Save size={16}/> Save</button><button className="btn primary"><Play size={16}/> Run simulation</button></div></div><div className="sim-layout"><section className="panel"><div className="panel-title"><Settings2 size={18}/><span>Experiment setup</span></div><label>Experiment name<input defaultValue={`${m.name} experiment`} /></label><label>Algorithm<select><option>Select an algorithm</option><option>FCFS</option><option>SJF</option><option>Round Robin</option><option>Priority</option></select></label><label>Input data<textarea placeholder="Enter values separated by commas..." /></label><button className="btn primary full-btn"><Play size={16}/> Run experiment</button><button className="reset"><RotateCcw size={15}/> Reset</button></section><section className="panel visualization"><div className="panel-title"><span>Live visualization</span><span className="live">● Ready</span></div><div className="empty-state"><div className="empty-icon"><Play size={28}/></div><h3>Your simulation will appear here</h3><p>Enter experiment data and run the simulation to watch the OS behavior step by step.</p></div></section></div><section className="explain"><Lightbulb size={21}/><div><b>Learning insight</b><p>This area will explain <strong>what happened and why</strong> after the simulator is connected to its algorithm engine.</p></div></section></div>}
